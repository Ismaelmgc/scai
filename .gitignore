# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/
.local/

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo

# Data — commit the small bootstrap files (OHLCV, model, portfolios, EDGAR);
# exclude only the 2.2GB feature matrix, which the pipeline regenerates each run.
data/raw/
data/interim/
data/processed/features_smallcap.parquet
data/paper_trading/logs/
data/paper_trading/**/archive_*/
# Paper-trading state now lives in Supabase (source of truth), not git.
# Keep model_registry.json tracked (small, low-churn model metadata).
data/paper_trading/portfolio.json
data/paper_trading/adaptive/portfolio.json
data/paper_trading/trades.parquet
data/paper_trading/adaptive/trades.parquet
data/paper_trading/signal_history.parquet
data/paper_trading/adaptive/signal_history.parquet
data/paper_trading/daily_log.jsonl
data/paper_trading/adaptive/daily_log.jsonl
data/paper_trading/signals_*.parquet
mlruns/

# Generated static dashboard (published to Pages as a build artifact, not committed)
site/

# Strategy reference doc (model + features summary) — LOCAL ONLY, never publish.
# Purged from git history 2026-06-13; keep it out of the public repo.
docs/strategy.html

# Environment
.env

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Test / coverage
htmlcov/
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
data/v3_benchmarks/cache/

# Research-only datasets + per-experiment benchmark results (regenerable; never
# merge to main — findings live in memory/v4-research-program.md + git history).
data/accum_features.parquet
data/residmom_features.parquet
data/pead_features.parquet
data/short_interest.parquet
data/insider_form4.parquet
data/earnings.parquet
data/v3_benchmarks/v4_feat_*.json
data/v3_benchmarks/v4_rm_*.json
data/research_breadth/
data/research/

# mid-cap swing experiment (research)
data/midcap/

# liquidcap: regenerable data gitignored; foundational data committed so CI is
# stateless (delisted slice = static anti-survivorship; EDGAR facts cache;
# model + meta persist across runs; membership + CIK overrides).
data/liquidcap/*.parquet
data/liquidcap/cache/
data/liquidcap/*.json
data/liquidcap/*.pkl
data/liquidcap/paper_trading/
data/liquidcap/*.html
!data/liquidcap/cik_overrides.json
!data/liquidcap/membership_sp500.parquet
!data/liquidcap/ohlcv_delisted.parquet
!data/liquidcap/edgar_facts_raw.parquet
!data/liquidcap/model_liquidcap.pkl
!data/liquidcap/model_meta.json
